apiVersion: v1
kind: PersistentVolume
metadata:
  name: model-volume
spec:
  storageClassName: local-storage
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/models"